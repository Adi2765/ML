import numpy as np

# Create a 1D NumPy array with floating-point numbers
array = np.array([1.01, 2.5, 3.99])

# Calculate the element-wise square root
print(np.sqrt(array))

# Round each element to the nearest integer
print(np.round(array))

# Print the mathematical constant pi (π)
print(np.pi)
